<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PathwayGames"
             xmlns:controls="clr-namespace:PathwayGames.Controls"
             x:Class="PathwayGames.Views.GameSelectionView" 
             Visual="Material"
             Title = "Select CPT Game">
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="plainButton" TargetType="Button">
                <Setter Property="TextColor" Value="White" />
                <Setter Property="FontSize" Value="Large" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="Margin" Value="10" />
                <Setter Property="Padding" Value="0,40" />
                <Setter Property="BorderWidth" Value="1" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="HorizontalOptions" Value="FillAndExpand" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout Spacing="0">
        <Grid RowSpacing="0" ColumnSpacing="0" Margin="10, 10, 10, 20" Padding="0"
            VerticalOptions="CenterAndExpand"
            HorizontalOptions="Fill">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Button Text="X" x:Name="TypeXButton" Image="" FontSize="Large" Command="{Binding TypeX}" Style="{StaticResource plainButton}"
                Grid.Column = "0" Grid.Row = "0" Grid.ColumnSpan="2" BackgroundColor="{StaticResource menu_green}"  HorizontalOptions="Center"
                    WidthRequest="{Binding Source={x:Reference TypeAXButton}, Path=Width}" 
                    HeightRequest="{Binding Source={x:Reference TypeXButton}, Path=Width}" />

            <Button Text="Seek X" x:Name="SeekXButton" Image="" FontSize="Large" Command="{Binding TypeX}" Style="{StaticResource plainButton}"
                    Grid.Column = "0" Grid.Row = "1" BackgroundColor="{StaticResource menu_blue}" 
                    HeightRequest="{Binding Source={x:Reference SeekXButton}, Path=Width}" />
            
            <Button Text="AX" x:Name="TypeAXButton" Image="" FontSize="Large" Command="{Binding TypeAX}" Style="{StaticResource plainButton}"
                    Grid.Column = "1" Grid.Row = "1" BackgroundColor="{StaticResource menu_pink}" 
                    HeightRequest="{Binding Source={x:Reference TypeAXButton}, Path=Width}" />

            <Button Text="Seek AX" x:Name="SeekAXButton" Image="" FontSize="Large" Command="{Binding TypeX}" Style="{StaticResource plainButton}"
                    Grid.Column = "0" Grid.Row = "2" BackgroundColor="{StaticResource menu_purple}"
                    HeightRequest="{Binding Source={x:Reference SeekAXButton}, Path=Width}" />

            <Button Text="Seek AX with Quiz" x:Name="SeekAXQuizButton" Image="" FontSize="Large" Command="{Binding TypeX}" Style="{StaticResource plainButton}"
                    Grid.Column = "1" Grid.Row = "2" BackgroundColor="{StaticResource menu_red}" 
                    HeightRequest="{Binding Source={x:Reference SeekAXQuizButton}, Path=Width}" />

        </Grid>
    </StackLayout>

</ContentPage>
