<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:g="clr-namespace:PathwayGames.Gestures;assembly=PathwayGames"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             x:Class="PathwayGames.Views.GameView"
             Title = "Type X CPT">
    <ContentPage.Content>
        <Grid Padding="10, 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="120"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>
 
            <ffimageloading:CachedImage x:Name="image" Source="{Binding SlideImageSource}" SuccessCommand="{Binding SlideDisplayedCommand}" 
                                        Aspect="AspectFit" VerticalOptions="Center" HeightRequest="400" Grid.Column="0" Grid.Row="0" />

            <Image Source="{Binding ButtonImageSource}" g:Gesture.Tapped="{Binding ButtonTappedCommand}" 
                   WidthRequest="160" VerticalOptions="EndAndExpand" Grid.Column="0" Grid.Row="1"/>

            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand" Grid.Column="0" Grid.Row="2">
                <Image Source="{Binding EngangementImageSource}" WidthRequest="18" />
                <Label >
                    <Label.FormattedText>
                        <FormattedString>
                            <Span Text="Slide " FontSize="Small" />
                            <Span Text="{Binding SlideIndex, Mode=OneWay}" FontSize="Small" />
                            <Span Text="/" FontSize="Small" />
                            <Span Text="{Binding SlideCount, Mode=OneWay}" FontSize="Small" />
                            <Span Text=" – " FontSize="Small" />
                            <Span Text="{Binding UserName, Mode=OneWay}" FontSize="Small" />
                            <Span Text=" – " FontSize="Small" />
                            <Span Text="{Binding Source={x:Static x:DateTime.Now}, StringFormat='{}{0:dd/MM/yyyy - hh:mm}'}" />
                            <Span Text=" – " FontSize="Small" />
                            <Span Text="{Binding Seed, Mode=OneWay}" FontSize="Small" />
                        </FormattedString>
                    </Label.FormattedText>
                </Label>
                <Image Source="{Binding RecordingImageSource}" WidthRequest="18" />
            </StackLayout>
        </Grid>
    </ContentPage.Content>

</ContentPage>